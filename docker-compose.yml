version: '3.7'
services:
  lint:
    image: buildkite/plugin-linter
    command: ['--id', 'cevoaustralia/azure-login']
    volumes:
      - ".:/plugin"

  tests:
    image: buildkite/plugin-tester
    volumes:
      - ".:/plugin"